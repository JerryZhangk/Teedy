<?xml version="1.0"?>
<ruleset name="Custom Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        This is a custom set of rules for Java code analysis.
    </description>

    <!-- Rule to check for too many lines of code in a class -->
    <rule ref="category/java/design.xml/NcssCount">
        <properties>
            <property name="minimum" svalue="50"/>
        </properties>
    </rule>

    <!-- Rule to check for high cyclomatic complexity -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="reportLevel" value="10"/>
        </properties>
    </rule>

    <!-- Rule to check for excessive coupling between objects -->
    <rule ref="category/java/design.xml/CouplingBetweenObjects">
        <properties>
            <property name="reportLevel" value="10"/>
        </properties>
    </rule>

    <!-- Rule to identify potential 'God Classes' -->
    <rule ref="category/java/design.xml/GodClass">
        <properties>
            <property name="reportLevel" value="1.0"/>
        </properties>
    </rule>

    <!-- Add more rules here as needed -->

</ruleset>
